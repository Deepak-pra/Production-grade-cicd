pytest==8.2.2
flake8==7.1.0
pytest-cov==5.0.0
build==1.2.1

# Clean runtime dependency used only to make dependency scanning meaningful.
# Do not use old vulnerable versions like requests==2.18.0.
requests==2.34.2
