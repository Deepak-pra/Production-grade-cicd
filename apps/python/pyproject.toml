[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "shipfast-api"
version = "1.0.0"
description = "Shipfast API Python app"
requires-python = ">=3.11"