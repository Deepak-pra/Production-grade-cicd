{
  "name": "shipfast-api-node",
  "version": "1.0.0",
  "description": "Node.js app for GHA CI/CD training demos",
  "main": "src/utils.js",
  "scripts": {
    "build": "npm pack --pack-destination dist/",
    "test": "jest --coverage",
    "lint": "eslint src/ --ext .js"
  },
  "dependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.57.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "src/**/*.js"
    ]
  }
}